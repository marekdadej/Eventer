@page
@model LoginWith2faModel
@{
    ViewData["Title"] = "Weryfikacja dwuetapowa";
}

<h1>@ViewData["Title"]</h1>
<hr />
<p>Twoje logowanie jest chronione aplikacją uwierzytelniającą. Wpisz kod z aplikacji poniżej.</p>
<div class="row">
    <div class="col-md-4">
        <form method="post" asp-route-returnUrl="@Model.ReturnUrl">
            <input asp-for="RememberMe" type="hidden" />
            <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
            <div class="form-floating mb-3">
                <input asp-for="Input.TwoFactorCode" class="form-control" autocomplete="off" />
                <label asp-for="Input.TwoFactorCode" class="form-label">Kod weryfikacyjny</label>
                <span asp-validation-for="Input.TwoFactorCode" class="text-danger"></span>
            </div>
            <div class="checkbox mb-3">
                <label asp-for="Input.RememberMachine" class="form-label">
                    <input asp-for="Input.RememberMachine" class="form-check-input" />
                    Zapamiętaj to urządzenie
                </label>
            </div>
            <div>
                <button type="submit" class="w-100 btn btn-lg btn-primary">Zaloguj się</button>
            </div>
        </form>
    </div>
</div>
<p>
    Nie masz dostępu do aplikacji?
    <a id="recovery-code-login" asp-page="./LoginWithRecoveryCode" asp-route-returnUrl="@Model.ReturnUrl">Zaloguj się za pomocą kodu odzyskiwania</a>.
</p>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}